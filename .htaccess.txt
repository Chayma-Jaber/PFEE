<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /tanithub/fr/

  # Si fichier ou dossier existe
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Sinon rediriger vers index.html
  RewriteRule ^ index.html [L]
</IfModule>